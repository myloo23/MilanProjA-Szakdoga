# Intentionally empty.
#
# Its presence marks this directory as pytest's rootdir and puts it on
# sys.path, so `pytest` works the same as `python -m pytest` from a clean
# clone. Not copied into the Docker image (the Dockerfile only copies app/).
